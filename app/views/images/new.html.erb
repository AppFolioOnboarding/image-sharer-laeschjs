<%= link_to "Go Back Home", root_path, class: "btn btn-primary" %>
<%= simple_form_for @image do |f| %>
  <%= f.input :url, label: 'Enter the Image URL:'%>
  <%= f.input :tag_list, label: 'Image Tag:' %>
  <%= f.button :submit, 'Save Image'%>
<% end %>
